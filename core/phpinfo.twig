{% extends parent_template %}

{% block title %}
    {{ Lang.get('core.php_info') }}
{% endblock %}

{% block content %}

    <iframe src="{{ route('core.operator.phpinfo.show') }}"
            frameborder="0"
            scrolling="yes"
            onload="(function(e){e.style.height = e.contentWindow.document.body.scrollHeight + 32 + 'px'})(this)"
            class="sp-w-full"></iframe>

{% endblock %}
